services:
  aliyun-ddns:
    image: ghcr.io/<your-github-username>/aliyun-ddns:latest
    container_name: aliyun-ddns
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    command: ["python", "/app/ddns_updater.py", "--config", "/app/config.yaml"]
